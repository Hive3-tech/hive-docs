import type { EndpointCardData } from "./onboarding-link-setup-endpoint-card";
import { onboardEndpointCard } from "./onboarding-link-setup-endpoint-data-onboard";

export const onboardingEndpointCards: EndpointCardData[] = [
  onboardEndpointCard,
];
